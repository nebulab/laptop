fancy_echo() {
  printf "\n%b\n" "$1"
}

brew_install() {
  if brew list -1 | grep -q "^${1}\$"; then
    fancy_echo "Package '$1' is installed"
  else
    fancy_echo "Installing '$1' ..."
    brew install $1
  fi
}
